export function Header() {
  return (
    <header className="header">
      <div className="header-title">Dashboard</div>
      <div className="header-actions">
        {/* plus tard: sélecteur de cluster, user menu, etc. */}
      </div>
    </header>
  );
}